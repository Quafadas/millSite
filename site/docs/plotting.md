
# Mermaid

Works straight out the box with Laika, so just insert a diagram and watch it live reload - put this markdown in your md file.

````markdown
```mermaid
flowchart LR
    draw[Draw] --> iterate[Iterate] --> profit[Profit]
```
````

```mermaid
flowchart LR
    draw[Draw] --> iterate[Iterate] --> profit[Profit]
```